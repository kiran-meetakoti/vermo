# Streamlit Community Cloud installs from this file (it takes precedence over
# pyproject.toml there). Local development uses `uv sync` + pyproject.toml —
# keep the two in step when bumping versions.
#
# Only what streamlit_app.py and its imports need; the FastAPI service
# (fastapi/uvicorn) is not part of the hosted Streamlit deployment.
streamlit==1.44.1
plotly>=6.8.0
pypdf>=6.14.2
pandas>=2.3.3
psycopg[binary,pool]>=3.2
